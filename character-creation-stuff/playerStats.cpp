//header
#include "cc-include.h"
#include <iostream>
#include <iomanip>

Stats playerStats(Stats st_playerStats){

Stats playerStats;

char confirmation = 'n';

int statsTotal = playerStats.strg + playerStats.prcptn + playerStats.end + playerStats.intgl + playerStats.agl + playerStats.luck + playerStats.magic;


while(confirmation == n || confirmation == N){
    while(statsTotal != 45){
    cout << "========================" << endl;
    cout <<  "\tPLAYER STATS" << endl;
    cout << "========================" << endl <<endl;
    




};



return st_playerStats;
}
